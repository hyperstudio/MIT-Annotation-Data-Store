(function (global, Tempus, ArSlice, TYPE_STRING, TYPE_FUNCTION, TYPE_NUMBER, TYPE_ARRAY, TYPE_REGEXP, undef) {
    
    function TempusInterval(from, to) {
        this._dateFrom = new Tempus(from);
        this._dateTo = this._dateFrom.clone().set(to);
        return this;
    };
    
    TempusInterval.prototype = {
        
        valueOf: function () {
            return +(this.toDateObj) - +(this.fromDateObj);
        },
        
        toString: function (format) {
              
        },
        
        toFuzzyString: function (reallyFuzzy) {
            
        },
        
        toISO8601IntervalString: function () {
            
        },

        
    };
    
    Tempus.Interval = TempusInterval;

}(global, Tempus, ([]).slice, 'string', 'function', 'number', 'array', 'regexp'));